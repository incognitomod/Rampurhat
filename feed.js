// Placeholder for loading merchant listings into the feed
document.addEventListener('DOMContentLoaded', () => {
  const container = document.getElementById('feedContainer');
  container.innerHTML += '<div>Sample Restaurant - Merchant Listing</div>';
});
