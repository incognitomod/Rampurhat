// Placeholder for merchant CRUD operations
document.addEventListener('DOMContentLoaded', () => {
  const merchantItems = document.getElementById('merchantItems');
  merchantItems.innerHTML = '<p>No items yet. Add one!</p>';
});
